<div class="col-sm-12">
  {{#if title}}
    <h2>{{title}}</h2>
  {{/if}}
  
  <table class="table">
    <thead>
      <tr>
        {{#each columnNames}}
          <th>{{this}}</th>
        {{/each}}
      </tr>
    </thead>

    <tbody></tbody>

  <!--
    <tfoot>
      <tr>
        <td colspan="3">some footer information</td>
      </tr>
    </tfoot>
  -->
  </table>
</div>
